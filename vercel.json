{
  "buildCommand": "pnpm expo export --platform web --max-workers 1",
  "outputDirectory": "dist",
  "devCommand": "npx expo start --https",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}